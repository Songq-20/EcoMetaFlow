You are working on EcoMetaFlow, a Python CLI bioinformatics workflow manager. The current project is v0.1.0, a dry-run skeleton generated by Cursor, with all tests passing.

Your task is to implement v0.2.0 enhancements while keeping v0.1.0 stable. Do NOT execute real bioinformatics tools or download databases.

---

## Goals

1. For each module (virus_prediction, mag_pipeline, read_based_risk, micro_risk), generate **realistic shell commands** for each analysis step.
2. Update `installer.py` to include **dry-run Conda commands** for missing tools according to `envs.yaml`.
3. Dry-run commands should **only print** what would be executed, not actually run anything.
4. Update tests to validate the generated commands.
5. Keep the existing directory structure, CLI arguments, and dry-run interface unchanged.
6. Do not add Docker, Snakemake, Nextflow, web UI, or database downloading in this version.
7. Ensure code is **beginner-friendly**, with comments explaining logic.
8. Keep Python package installation compatible with pip (`pip install -e .`) and future conda packaging.

---

## Deliverables

1. **Modified or new files**
   - `src/ecometa_flow/runner.py`
   - `src/ecometa_flow/commands.py`
   - `src/ecometa_flow/installer.py`
   - Any new test files needed to validate command generation
2. **Examples**
   - Dry-run shell commands for at least one sample in each module
   - Updated installer dry-run output
3. **Tests**
   - pytest-compatible tests verifying that dry-run commands are generated correctly
4. **Summary**
   - List of all files modified or added
   - Description of what was added or changed in each file

---

## Constraints

- Do NOT actually call Trimmomatic, MEGAHIT, VirSorter2, CheckV, GTDB-Tk, Kraken2, Bowtie2, Samtools, CoverM, or other external bioinformatics tools.
- Do NOT install or download any databases.
- Only generate commands that would be run if the tools were present.
- Keep CLI interface exactly as in v0.1.0.
- Use Python standard library plus PyYAML for configuration parsing.
- Include comments for every function and class explaining purpose.
- Do not reorganize existing code unless necessary for v0.2.0 functionality.
- Ensure all existing v0.1.0 tests still pass.

---

## Expected CLI Dry-Run Examples

```bash
# virus_prediction module
ecometa-flow run \
  -m virus_prediction \
  -i examples/raw_reads \
  -o work_virus \
  --envs examples/envs.yaml \
  -t 16 \
  --dry-run

# installer dry-run
ecometa-flow install --module virus_prediction --dry-run