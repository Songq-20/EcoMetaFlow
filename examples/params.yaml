# Optional workflow parameters (not used in v0.1.0 skeleton).
# Reserved for future versions.

trimming:
  leading: 3
  trailing: 3
  sliding_window: "4:15"
  min_length: 36

assembly:
  min_contig_len: 200

threads: 16
